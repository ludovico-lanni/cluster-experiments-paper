[project]
name = "cluster-experiments-paper"
version = "0.1.0"
description = "Code for the paper on the cluster-experiments python library"
requires-python = "<3.13,>=3.9"

dependencies = [
    "cluster-experiments==0.27.0",
    "matplotlib>=3.9.4",
    "pyarrow>=21.0.0",
    "seaborn>=0.13.2",
]
